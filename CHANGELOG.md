# Changelog

## Unreleased — Week7 platform baseline

- Added canonical three-role lifecycle, preflight, status, log retention, and
  clean-host reproduction tooling.
- Added standardized single-turn evaluation JSONL/CSV schema and historical
  2B/8B baseline summaries.
- Rebuilt the Server PC platform console around full-chain node health and
  latency, including separate recording and ASR-inference timing.
- Added AI Max VLM input/prompt/JSON inspection and fixed host llama-process
  monitoring.
- Hardened TB3 bringup discovery and Xorg/Openbox/Epiphany lifecycle while
  preserving `dry_run=true` as the default.
- Froze the 14-row P4 demo matrix, added dry-run execution and summarization,
  and hardened Japanese move-then-observe plus autonomous-navigation boundaries.
- Added repository audit, validation, CI, hardware, limitations, and release
  documentation.
- Passed a canonical three-role deployment check and targeted live I/O smoke
  covering real ASR/camera, VLM/validation, TTS/playback, face UI, and bounded
  motion with final stop; retained one recoverable ASR timeout as known evidence.
- Repositioned the P4 test table as the formal-presentation run sheet and closed
  Week7 without treating a repeated full physical showcase as an engineering
  exit gate.

Formal presentation execution and final release publication remain separate
operator decisions.
